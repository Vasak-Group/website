image: node:latest

cache:
    paths:
        - node_modules/

pages:
  script:
  - npm install -g @angular/cli
  - npm install
  - npm run build
  - mv dist/LSDWeb public
  artifacts:
    paths:
    - public
  only:
  - master
